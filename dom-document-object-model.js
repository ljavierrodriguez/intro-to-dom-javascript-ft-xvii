/* 

document: Permite el acceso a toda la informacion del documento y sus atributos (metodos)

getElementById('id') : Retorna el elemento con el id especificado
getElementsByClassName('class') : Retorna un conjunto de elementos segun la clase especificada ([HTMLElement, ...]) 
getElementsByName('name') : Retorna un conjunto de elementos segun el nombre del elemento
getElementsByTagName('tag') : Retorna un conjunto de elementos segun el elemento a buscar

querySelector('selector_css') : Retorna el primer elemento que consiga con ese selector
querySelectorAll('selector_css') : Retorna un conjunto de elementos por el selector buscado

createElement('tag') : Permite crear un nuevo elemento html segun etiqueta dada
createTextNode('texto'): Permite crear un nodo de texto

appendChild(HTMLElement): Permite añadir el elemento en el elemento seleccionado
innerHTML : Permite ver o sustituir el contenido del elemento seleccionado

removeChild(HTMLElement): Permite eliminar el elemento seleccionado

*/